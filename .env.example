# Email Configuration (Optional)
# Configure these to enable email delivery of encrypted reports

# SMTP Server Settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Email Credentials
SENDER_EMAIL=your-bot-email@example.com
SENDER_PASSWORD=your-app-specific-password

# Recipient
RECIPIENT_EMAIL=security@your-company.com
